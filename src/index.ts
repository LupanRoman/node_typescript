// import express from 'express';
// import cors from 'cors'
// import {config} from 'dotenv';
// import mongoose from 'mongoose';

// * Uncomment to initialize the server
//  const app = express();
// * Parse the incoming requests with JSON payloads
// app.use(express.json());
// * Using cors to get rid of the cors origin error in the request
/* 
    app.use(
        cors({
            origin: '*',
        })
    );
*/

// * Listen to the port of the server
// app.listen(PORT, () => {console.log(`Server is running on port ${PORT}`)})

// * Uncomment the line bellow ans set the desired port number
// const PORT =
